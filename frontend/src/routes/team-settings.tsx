import AgentTeamForm from "~/components/AgentTeamForm";

const TeamSettings = () => {
  return (
    <div>
      <AgentTeamForm onSubmit={() => {}} />
    </div>
  );
};

export default TeamSettings;
